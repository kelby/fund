<div class="container">
  <h1>Article Categories</h1>

  <table>
    <thead>
      <tr>
        <th>Name</th>
        <th>Slug</th>
        <th>Cover</th>
        <th>Intro</th>
        <th>Article catalog</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @article_categories.each do |article_category| %>
        <tr>
          <td><%= article_category.name %></td>
          <td><%= article_category.slug %></td>
          <td><%= article_category.cover %></td>
          <td><%= article_category.intro %></td>
          <td><%= article_category.article_catalog_id %></td>
          <td><%= link_to 'Show', article_category %></td>
          <td><%= link_to 'Edit', edit_article_category_path(article_category) %></td>
          <td><%= link_to 'Destroy', article_category, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to 'New Article Category', new_article_category_path %>
</div>
