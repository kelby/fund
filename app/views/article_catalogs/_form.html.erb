<%= form_for(article_catalog) do |f| %>
  <% if article_catalog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(article_catalog.errors.count, "error") %> prohibited this article_catalog from being saved:</h2>

      <ul>
      <% article_catalog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :slug %>
    <%= f.text_field :slug %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
