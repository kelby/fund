<h1>
  <%= @panel_project.name %>

  <small><%= link_to 'Show', @panel_project %></small>
  <small class="pull-right">
    <%= link_to 'Back', search_panel_projects_path(name: @panel_project.name) %>
  </small>
</h1>

<%= render 'form', panel_project: @panel_project %>
